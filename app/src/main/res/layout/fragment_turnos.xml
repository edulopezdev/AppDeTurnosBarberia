<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@drawable/background_gradient"
  android:paddingTop="24dp"
  android:paddingStart="24dp"
  android:paddingEnd="24dp"
  tools:context=".ui.turnos.TurnosFragment"
>
  <!-- Logo -->
  <ImageView
    android:id="@+id/ivLogoTurnos"
    android:layout_width="120dp"
    android:layout_height="120dp"
    android:src="@drawable/logo_dorado"
    android:contentDescription="Logo"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
  />
  <!-- SwipeRefreshLayout (ahora debajo del logo) -->
  <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
    android:id="@+id/swipeRefrescarTurnos"
    android:layout_width="327dp"
    android:layout_height="506dp"
    android:layout_marginTop="12dp"
    android:layout_marginBottom="90dp"
    app:layout_constraintTop_toBottomOf="@id/ivLogoTurnos"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintHorizontal_bias="0.489"
    app:layout_constraintVertical_bias="0.0"
    app:layout_constraintWidth_percent="0.9"
  >
    <androidx.recyclerview.widget.RecyclerView
      android:id="@+id/rvTurnos"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:clipToPadding="false"
      android:paddingBottom="16dp"
    />
  </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
  <!-- Mensaje cuando no hay turnos -->
  <TextView
    android:id="@+id/tvNoTurnos"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:text="Aún no tenés turnos. Presioná el botón para agendar uno."
    android:textColor="#E6FFFFFF"
    android:textSize="16sp"
    android:gravity="center"
    android:padding="16dp"
    android:visibility="gone"
    tools:visibility="visible"
    app:layout_constraintTop_toTopOf="@id/swipeRefrescarTurnos"
    app:layout_constraintBottom_toBottomOf="@id/swipeRefrescarTurnos"
    app:layout_constraintStart_toStartOf="@id/swipeRefrescarTurnos"
    app:layout_constraintEnd_toEndOf="@id/swipeRefrescarTurnos"
  />
  <!-- FAB Crear turno -->
  <com.google.android.material.floatingactionbutton.FloatingActionButton
    android:id="@+id/fabCrearTurno"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:contentDescription="Crear turno"
    app:srcCompat="@android:drawable/ic_input_add"
    app:tint="@color/white"
    android:backgroundTint="@color/colorPrimary"
    android:layout_marginEnd="24dp"
    android:layout_marginBottom="24dp"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
  />
</androidx.constraintlayout.widget.ConstraintLayout>
